---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: bgprouters.routing.osd.redhat.com
spec:
  group: routing.osd.redhat.com
  names:
    kind: BGPRouter
    listKind: BGPRouterList
    plural: bgprouters
    singular: bgprouter
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.nodeName
      name: Node
      type: string
    - jsonPath: .spec.gceInstance
      name: Instance
      type: string
    - jsonPath: .spec.gceZone
      name: Zone
      type: string
    - jsonPath: .status.ipAddress
      name: IP
      type: string
    - jsonPath: .status.nccSpokeName
      name: Spoke
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BGPRouter is the Schema for the bgprouters API.
          Each BGPRouter represents a single Kubernetes node elected as a BGP router.
          These resources are controller-managed via ownerReferences to BGPRoutingConfig.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              BGPRouterSpec identifies the GCE instance backing this BGP router.
              This is controller-populated; users should not create BGPRouter resources.
            properties:
              gceInstance:
                description: GCEInstance is the GCE instance name.
                minLength: 1
                type: string
              gceZone:
                description: GCEZone is the GCE zone of the instance.
                minLength: 1
                type: string
              nodeName:
                description: NodeName is the Kubernetes node name.
                minLength: 1
                type: string
            required:
            - gceInstance
            - gceZone
            - nodeName
            type: object
          status:
            description: BGPRouterStatus defines the observed state of a single BGP
              router node.
            properties:
              bgpPeers:
                description: BGPPeers lists the Cloud Router BGP peer names associated
                  with this router.
                items:
                  type: string
                type: array
              canIpForward:
                description: CanIPForward indicates whether IP forwarding is enabled
                  on the GCE instance.
                type: boolean
              conditions:
                description: Conditions represent the latest available observations
                  of this router's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              frrConfigurationName:
                description: FRRConfigurationName is the name of the FRRConfiguration
                  CR created for this router.
                type: string
              gceInstanceLink:
                description: GCEInstanceLink is the fully-qualified GCE instance self-link.
                type: string
              ipAddress:
                description: IPAddress is the node's internal IP address.
                type: string
              lastUpdated:
                description: LastUpdated is the time when this status was last written.
                format: date-time
                type: string
              nccSpokeName:
                description: NCCSpokeName is the NCC spoke this router belongs to.
                type: string
              nestedVirtualization:
                description: |-
                  NestedVirtualization indicates whether nested virtualization is enabled.
                  nil means the feature is not managed.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
